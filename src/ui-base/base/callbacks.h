#ifndef CALLBACKS_H
#define CALLBACKS_H

namespace UiBase
{
    enum class Callbacks
    {
        MouseOver,
        Clicked,
        Dragged,
        Dropped,
        ScrolledHorizontally,
        ScrolledVertically
    };
}

#endif // CALLBACKS_H
