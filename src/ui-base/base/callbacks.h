#ifndef CALLBACKS_H
#define CALLBACKS_H

namespace UiBase
{
    enum class Callbacks
    {
        MouseOver,
        Clicked,
        Draged,
        Dropped,
        ScrolledHorizontally,
        ScrolledVertically
    };
}

#endif // CALLBACKS_H
